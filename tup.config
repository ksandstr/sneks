# options about debugging. release variants should have all set to 'n', but why
# would there be release builds of sneks. CONFIG_DEBUG dominates the other
# DEBUG options by daemonic birthright.
CONFIG_DEBUG=y
CONFIG_MORE_DEBUG=n
CONFIG_JUST_DEBUG_ME_UP=n
CONFIG_SELFTEST=y
